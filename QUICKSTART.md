# Швидкий старт - Гімназія №4

## 📦 Що потрібно?
- GitHub акаунт (безкоштовно)
- Google акаунт для Firebase (безкоштовно)
- 15 хвилин часу

## 🚀 Крок за кроком

### 1️⃣ GitHub (3 хв)
1. Створіть репозиторій: github.com/new
2. Завантажте файли: `index.html`, `styles.css`, `app.js`
3. Settings → Pages → main branch → Save
4. Готово! Сайт: `https://ваш-юзернейм.github.io/назва-репо`

### 2️⃣ Firebase (7 хв)
1. console.firebase.google.com → Create Project: `gym4-nutrition`
2. Realtime Database → Create → Europe → Test mode
3. Settings → Web app → Copy config
4. Database → Rules → вставте:
```json
{
  "rules": {
    "classes": {".read": true, ".write": true},
    "reports": {".read": true, ".write": true},
    "config": {".read": true, ".write": true}
  }
}
```

### 3️⃣ З'єднання (3 хв)
В `app.js` замініть:
```javascript
FIREBASE_CONFIG: {
    apiKey: "ВАШ_API_KEY",
    authDomain: "gym4-nutrition.firebaseapp.com",
    databaseURL: "https://gym4-nutrition-default-rtdb.europe-west1.firebasedatabase.app",
    projectId: "gym4-nutrition",
    storageBucket: "gym4-nutrition.appspot.com",
    messagingSenderId: "ВАШ_ID",
    appId: "ВАШ_APP_ID"
}
```

Збережіть → git push

## ✅ Перевірка
1. Відкрийте сайт
2. Має бути зелений "CLOUD SYNC"
3. Вхід → Адмін (PIN: 1312)
4. Додайте клас → оновіть сторінку
5. Якщо клас залишився - все працює! 🎉

## 🔐 Безпека
**Одразу змініть PIN-коди!**
- Адмін → Безпека → Змініть PIN їдальні
- В `app.js` → `ADMIN_PIN: '1312'` → змініть

## 📱 Використання

**Вчитель:**
1. Клас. керівник → PIN класу
2. Заповнити форму → Відправити

**Їдальня:**
1. Працівник їдальні → PIN: 5555
2. Перегляд заявок → Друк/CSV

**Адмін:**
1. Адміністратор → PIN: 1312
2. Керування класами/звітами

## ❓ Проблеми?

**Offline режим:**
- Перевірте URL worker в `app.js`
- Перевірте CORS у worker

**PIN не працює:**
- 4 цифри точно
- Перевірте в адмін-панелі

**Дані не зберігаються:**
- F12 → Console → перевірте помилки
- Видаліть cookies → спробуйте знову

## 💡 Поради
- Робіть backup раз на тиждень
- Тестуйте зміни локально
- Використовуйте різні PIN для класів
- CSV файли відкриваються в Excel

---
**Документація:** README.md  
**Деплой:** DEPLOY.md  
**Підтримка:** F12 → Console для помилок
